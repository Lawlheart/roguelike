// Configurable Settings
export const DEFAULT_MAP_SETTINGS = {
  runs: 500,
  minRoomSize: 2,
  maxRoomSize: 6,
}

// Initial States
export const MAP_INITIAL_STATE = {
  map: [],
  coords: [],
  potions: [],
}